using System.Collections.Generic;
using System.Drawing;
using AForge.Math;

namespace CameraVision
{
    public class SpaceReconstructor
    {
        public List<Vector3> Reconstruct3DSpace(Bitmap first, Bitmap second)
        {
            return null;
        }
    }
}
